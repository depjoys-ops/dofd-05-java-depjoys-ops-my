name: Java CI with Maven

on:
  push:
    branches: 
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    
    - name: Clone project to WORKSPACE
    - uses: actions/checkout@v3
    - run: ls -la
    
